#include "main.h"
/**
 * isupper - check for uppercase character
 * c: variable text
 * Rerturn: to 1 when uppercase and 0 when lowwercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
return (1);
}
else
{
	return (0);
}
}
